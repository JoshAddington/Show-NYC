{
  "config": {
    "unsafe-perm":true
  },
  "repository": "https://github.com/angular/angular-seed",
  "dependencies": {
    "bower": "^1.5.1"
  },
  "scripts": {
      "postinstall": "bower install --allow-root"
  }
}
